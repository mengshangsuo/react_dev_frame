import React from 'react'
// import { useParams, withRotuer } from 'react-router'

interface Props { }

function News(props: Props) {
  // const params = useParams()
  // console.log('params', params);
  return (
    <h2>新闻动态</h2>
  )
}

export default (News)
